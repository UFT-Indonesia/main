# Copy this file to .env and fill in the real values.
# Never commit .env — it is already listed in .gitignore.

ConnectionStrings__Default=Host=localhost;Port=5432;Database=localhost;Username=localhost;Password=localhost
Jwt__SigningKey=<REPLACE_WITH_64_BYTE_RANDOM_BASE64_SECRET>
DeviceIngest__HmacSecret=<REPLACE_WITH_DEVICE_HMAC_SECRET>
